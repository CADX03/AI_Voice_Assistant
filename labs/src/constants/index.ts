import icons from "./icons.ts";
import images from "./images.ts";
import models, {
    sttOptions,
    llmOptions,
    ttsOptions,
    languageOptions,
    paramsNames,
    paramsIcons
  } from "./models.ts";

import type {
    ModelProps,
    STTProps,
    LLMProps,
    TTSProps,
    LanguageProps,
    ConfigParams
} from "./models.ts";

export {
    icons,
    images,
    models,
    sttOptions,
    llmOptions,
    ttsOptions,
    languageOptions,
    paramsNames,
    paramsIcons,
};
  
export type {
    ModelProps,
    STTProps,
    LLMProps,
    TTSProps,
    LanguageProps,
    ConfigParams
};
